version: 2

sources:
  - name: bronze
    schema: bronze
    tables:
      - name: events
        description: Raw web events ingested from Redpanda topic raw.events
        freshness:
          warn_after: { count: 1, period: hour }
      - name: orders
        description: Raw order data ingested from Redpanda topic raw.orders
        freshness:
          warn_after: { count: 1, period: hour }
